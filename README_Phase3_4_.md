# Phase 3~4 — 공적분 검정 및 모형 추정

> **작성일**: 2026-04-24  
> **목적**: Phase 3(Johansen 공적분 검정)과 Phase 4(VAR/VECM 추정 + IRF 기준선 산출)의 목적, 코드 역할, 실행 방법, 결과 해석을 정리

---

## 1. 개요

Phase 3은 구간별 시계열 쌍의 장기 균형 관계(공적분) 존재 여부를 검정하여 VECM 또는 VAR 모형 경로를 결정하는 단계이다. Phase 4는 결정된 경로에 따라 모형을 추정하고, IRF(충격반응함수)에서 정상 전달 시차와 전이탄력성을 산출하는 단계이다.

```
Phase 2 산출물 (integration_orders.json)
Phase 1 산출물 (seasonal_adjusted, changes)
    │
    ▼
[Phase 3] Johansen 공적분 검정
    ├─ 구간별 시계열 쌍에 Trace + Max-Eigen 통계량 검정
    ├─ 공적분 있음 → VECM 경로
    ├─ 공적분 없음 → VAR 경로
    └─ model_routing.json 생성
    │
    ▼
[Phase 4] 모형 추정 및 기준선 산출
    ├─ VECM 구간: 수준 데이터로 VECM 추정, ECT 산출
    ├─ VAR 구간: 변화율 데이터로 VAR 추정, 로그 스프레드 산출
    ├─ IRF 산출 → 피크 시점 = 정상 전달 시차, 피크 크기 = 전이탄력성
    └─ 기준선 테이블 생성 (Phase 7 이상 탐지 기준)
    │
    ▼
Phase 5 (Granger 인과 방향 확정) / Phase 6 (구조 변화 탐지) 입력
```

---

## 2. 실행 방법

### 전제 조건

Phase 1~2가 완료되어 다음 파일이 존재해야 한다:
- `data/processed/phase1/seasonal_adjusted/{cid}_sa.csv`
- `data/processed/phase1/changes/{cid}_changes.csv`
- `data/processed/phase2/integration_orders.json`
- `data/processed/product_config.json`

### Phase 3 실행

```bash
python src/preprocessing/phase3_cointegration_test.py
```

### Phase 4 실행

```bash
python src/preprocessing/phase4_model_estimation.py
```

Phase 3 → Phase 4 순서로 실행한다. Phase 4는 Phase 3의 `model_routing.json`을 입력으로 사용한다.

---

## 3. Phase 3 — Johansen 공적분 검정

### 3.1 목적

두 가격 시계열(상류, 하류)이 각각 비정상(I(1))이더라도 둘 사이에 장기 균형 관계가 존재하는지 확인한다. 공적분이 있으면 두 가격이 일시적으로 벌어져도 결국 균형으로 돌아온다는 뜻이며, 이 경우 VECM으로 장기 균형 복귀 속도를 포함한 모형을 추정할 수 있다. 공적분이 없으면 VAR로 단기 동적 관계만 추정한다.

### 3.2 검정 방법

**Johansen 검정** (Engle-Granger 대비 장점):
- 종속변수 선택 문제 없음 (시스템 전체를 한꺼번에 검정)
- 공적분 벡터의 개수까지 파악 가능
- 2변수 시스템에서도 더 강건

**검정 통계량 2종 병행**:
- **Trace 통계량**: "공적분이 r개 이하" 귀무가설. 통계량 > 임계값(5%)이면 기각.
- **Max-Eigenvalue 통계량**: "공적분이 정확히 r개" 검정. 통계량 > 임계값이면 기각.

**판정 로직**:

| Trace | Max-Eigen | 판정 | 근거 |
| --- | --- | --- | --- |
| 기각 | 기각 | **공적분 확정** | 둘 다 일치 |
| 기각 | 미기각 | **공적분** | Trace 우선 (표본 크기에 더 강건) |
| 미기각 | 기각 | **공적분 없음** | 보수적 처리 |
| 미기각 | 미기각 | **공적분 없음** | 둘 다 일치 |

**파라미터**:

| 파라미터 | 값 | 근거 |
| --- | --- | --- |
| det_order | 0 | 상수항 포함, 추세 미포함. 가격 전달의 장기 균형에 일정 마진 존재 가정 |
| k_ar_diff | VAR 최적 시차(AIC) - 1, 최소 1 | VECM 시차 = VAR 시차 - 1 |
| 유의수준 | 5% | 임계값 테이블 인덱스 1 |

**비고**: Johansen 검정은 ADF와 달리 p값을 반환하지 않는다. 통계량이 임계값을 초과하면 귀무가설(공적분 없음)을 기각한다.

### 3.3 적분 차수 호환성

구간 쌍의 두 시계열이 모두 I(1)이면 검정이 가장 이상적이다. I(2) 시계열이 포함된 구간은 주의 플래그(`integration_flag`)를 부착하고 검정을 진행한다.

플래그가 부착된 구간 (Phase 2에서 확인된 I(2) 2건):
- 땅콩 구간 C: ppi=I(1), wholesale_price=I(2)
- 땅콩 구간 D: wholesale_price=I(2), cpi=I(1)
- 오렌지 구간 A: intl_price_krw=I(2), import_price_usd=I(1)

### 3.4 검정 결과

33개 구간 쌍 검정 결과:

| 모형 | 개수 | 비율 |
| --- | --- | --- |
| VECM (공적분 있음) | 19개 | 57.6% |
| VAR (공적분 없음) | 14개 | 42.4% |
| 적분 차수 주의 플래그 | 3개 | — |

과반수에서 공적분이 확인된 것은 이 프로젝트에 유리한 결과이다. VECM 구간에서는 ECT를 직접 산출할 수 있어서 패턴 3(스프레드 누적 확대)의 탐지 정밀도가 올라가고, 패턴 2(비대칭 전달)의 TECM 검정도 적용 가능해진다.

### 3.5 Trace 통계량 분포

| 범위 | 구간 수 | 판정 | 해당 구간 |
| --- | --- | --- | --- |
| 30 이상 | 8개 | VECM | 밀·옥수수·대두·설탕 구간 A, 오렌지 구간 D 등 (강한 공적분) |
| 20~30 | 7개 | VECM | 팜유 구간 A·B, 땅콩 구간 A 등 (안정적 공적분) |
| 15.49~20 | 4개 | VECM | 옥수수 D'(17.50), 바나나 B(17.57) 등 (경계적 공적분) |
| 12~15.49 | 7개 | VAR | 커피 A(12.08), 쇠고기 A(12.98), 땅콩 B(14.45) 등 (아슬하게 미기각) |
| 12 미만 | 7개 | VAR | 설탕 D'(3.45), 커피 D'(7.13) 등 (명확한 공적분 부재) |

경계 사례가 양쪽에 존재한다. VECM 쪽에서 옥수수 D'(17.50)과 바나나 B(17.57)는 임계값(15.49)을 겨우 넘겼고, VAR 쪽에서 땅콩 B(14.45)와 커피 B(13.82)는 임계값에 근접했다. 이 경계 사례들은 표본 크기나 시차 설정에 따라 결과가 달라질 수 있는 민감한 구간이다.

### 3.6 구간별 공적분 비율

| 구간 | VECM | VAR | 공적분 비율 | 해석 |
| --- | --- | --- | --- | --- |
| A (국제가→수입단가) | 6개 | 4개 | 60% | 곡물 선물 시장 기반 품목에서 강함 |
| B (수입단가→PPI) | 7개 | 3개 | 70% | 전 구간 중 가장 높음. 가공·제조 단계 전달 경로가 가장 뚜렷 |
| C (PPI→도매가) | 2개 | 1개 | 67% | 바나나·오렌지 공판장 기반 거래가 안정적 |
| D/D' (도매가/PPI→CPI) | 4개 | 6개 | 40% | 전 구간 중 가장 낮음. 소매 단계 유통 마진의 완충 효과 |

구간 B의 공적분 비율이 70%로 가장 높다. 수입 원자재가 국내 가공·제조 단계를 거치면서 가격이 전달되는 경로가 가장 안정적이라는 뜻이다. 구간 D/D'가 40%로 가장 낮은 것은 소매 단계의 유통 마진·광고비·포장비·소매업체 가격 전략이 생산자 물가와 소비자 물가 사이의 장기 균형을 약화시키는 구조적 특성을 보여준다.

### 3.7 품목 유형별 패턴

**품목별 모형 선택 요약**:

| 품목 | 구간 A | 구간 B | 구간 C | 구간 D/D' | 유형 |
| --- | --- | --- | --- | --- | --- |
| 밀 | VECM | VECM | — | VECM | 전 구간 VECM |
| 옥수수 | VECM | VECM | — | VECM | 전 구간 VECM |
| 대두 | VECM | VECM | — | VAR | 상류 VECM, 하류 VAR |
| 팜유 | VECM | VECM | — | VAR | 상류 VECM, 하류 VAR |
| 설탕 | VECM | VECM | — | VAR | 상류 VECM, 하류 VAR |
| 커피 | VAR | VAR | — | VAR | 전 구간 VAR |
| 쇠고기 | VAR | VAR | — | VAR | 전 구간 VAR |
| 땅콩 | VECM | VAR | VAR⚠️ | VAR⚠️ | A만 VECM |
| 바나나 | VAR | VECM | VECM | VECM | A만 VAR |
| 오렌지 | VAR⚠️ | VECM | VECM | VECM | A만 VAR |

(⚠️ = I(2) 포함 구간, 적분 차수 주의 플래그)

5가지 유형으로 분류된다:

**전 구간 VECM (밀, 옥수수)**: 국제가→수입단가→PPI→CPI 전 경로에서 장기 균형이 확인된 유일한 품목들이다. 밀가루·전분이 비교적 단순 가공 제품이라 가격 전달 경로가 직접적이고, 300개월의 충분한 표본이 검정력을 뒷받침한다.

**상류 VECM, 하류 VAR (대두, 팜유, 설탕)**: 구간 A·B에서는 장기 균형이 있지만 구간 D'에서는 없다. 두부(대두)·식용유(팜유)·설탕의 CPI는 원재료 외에 포장·광고·유통 비용이 상당 부분을 차지하므로 PPI와의 장기 균형이 희석되는 것으로 해석된다.

**전 구간 VAR (커피, 쇠고기)**: 어떤 구간에서도 장기 균형이 확인되지 않는다. 커피는 관측치 61개로 전 품목 중 가장 짧아서 검정력 부족이 주 원인이다. 실제로 커피 구간 A의 Trace(12.08)는 임계값(15.49)의 78%에 달해, 100개월 이상의 표본이 있었다면 결과가 달랐을 가능성이 있다. 쇠고기는 300개월의 충분한 표본에도 불구하고 VAR로 나왔는데, Pink Sheet(뉴질랜드산)과 한국 수입(미국·호주산)의 원산지 불일치가 구조적 원인이다.

**A만 VAR, B·C·D VECM (바나나, 오렌지)**: 국제가→수입단가 관계는 약하지만, 국내 유통 단계(수입단가→PPI→도매가→CPI)에서는 장기 균형이 확인된다. 국제 열대 과일 시장은 산지별 가격 차이가 크고 물류 변동이 커서 장기 균형이 약하지만, 국내에 들어온 이후에는 공판장(가락시장) 기반 도매 거래가 안정적 전달 채널 역할을 한다는 해석이다.

**A만 VECM, 나머지 VAR (땅콩)**: 국제가→수입단가에서만 장기 균형이 있고 이후 구간은 전부 VAR이다. 구간 B에서 PPI가 '견과가공품' 합산이라 수입단가와의 직접적 연결이 약하고, 구간 C·D는 I(2) 문제(wholesale_price)가 겹쳐 공적분 검정의 전제 자체가 불완전하다.

### 3.8 I(2) 플래그 구간의 검정 결과

| 구간 | Trace | 임계값 | 결과 | 비고 |
| --- | --- | --- | --- | --- |
| 땅콩 C (ppi↔wholesale_price) | 10.18 | 15.49 | VAR | 임계값 대비 66% |
| 땅콩 D (wholesale_price→cpi) | 14.54 | 15.49 | VAR | 임계값 대비 94% — 경계 사례 |
| 오렌지 A (intl_price_krw→import_price_usd) | 9.68 | 15.49 | VAR | 임계값 대비 63% |

3건 모두 VAR로 나왔다. 다만 땅콩 구간 D(14.54)는 임계값(15.49)에 매우 근접하여, 적분 차수가 I(1)이었다면 VECM으로 판정됐을 가능성이 높다. 이 결과가 "실제로 장기 균형이 없어서"인지 "적분 차수 불일치로 인한 검정력 저하 때문"인지 구분이 어렵다는 한계가 있다.

### 3.9 시차 선택 결과

| VAR 최적 시차(AIC) | 구간 수 | 비율 |
| --- | --- | --- |
| 1 | 0개 | 0% |
| 2 | 8개 | 24.2% |
| 3 | 13개 | 39.4% |
| 4 | 12개 | 36.4% |

시차 3~4가 전체의 76%를 차지한다. 월별 데이터에서 국제가 변동이 국내 가격에 반영되기까지 3~4개월이 걸린다는 의미이며, 곡물 가공업체의 선도 구매 관행(3~7개월)과 일치하는 결과이다. 커피 D'만 시차 1로 유일하게 짧은데, 관측치 61개에서 긴 시차를 선택하면 유효 관측치가 크게 줄어들어 추정 안정성이 떨어지기 때문이다.

### 3.10 Phase 4·7 연계 시사점

**패턴 3 적용 범위**: 구간 B만 패턴 3의 대상이며, VECM 7개 구간에서는 ECT 기반으로, VAR 3개 구간(커피·쇠고기·땅콩)에서는 로그 스프레드 기반으로 스프레드 모니터링을 수행한다. VECM 비율이 70%라 대부분 이론적으로 정밀한 ECT 기반 분석이 가능하다.

**패턴 2 TECM 적용 범위**: TECM(비대칭 전달 검정)은 공적분이 있는 구간에서만 적용 가능하다. 구간 A·B에서 VECM인 13개 구간에 TECM을 적용하고, VAR인 7개 구간에는 비대칭 VAR을 적용한다.

**커피·쇠고기의 분석 제약**: 전 구간 VAR이므로 ECT 산출이 불가하고, 패턴 3은 로그 스프레드로 대체하며, 패턴 2는 비대칭 VAR로만 검정 가능하다. 커피는 표본 부족, 쇠고기는 원산지 불일치라는 구조적 원인이므로 데이터 확보만으로는 해결이 어려운 한계다.
---

## 4. Phase 4 — 모형 추정 및 기준선 산출

### 4.1 목적

Phase 3에서 결정된 경로(VECM/VAR)에 따라 모형을 추정하고, IRF에서 정상 전달 시차(Phase 7 패턴 1 기준)와 전이탄력성(패턴 2 참조)을 산출한다. 또한 ECT(VECM) 또는 로그 수준 스프레드(VAR)를 산출하여 Phase 7 패턴 3 및 Phase 7-ML 피처로 제공한다.

### 4.2 모형 추정 분기

**VECM 구간 (공적분 있음, 19개)**:
- 입력: 계절 조정 **수준** 데이터 (`_sa.csv`)
- 시차: `model_routing.json`의 `johansen_lag` 사용
- 산출: ECT(오차수정항) = 공적분 벡터(β) × 수준 데이터
- ECT는 장기 균형으로부터의 이탈을 직접 측정

**VAR 구간 (공적분 없음, 14개)**:
- 입력: 계절 조정 후 **변화율** 데이터 (`_changes.csv`)
- 시차: `model_routing.json`의 `var_lag_aic` 사용
- 산출: 로그 수준 스프레드 = log(하류_sa) - log(상류_sa)
- ECT 대체 지표로 Phase 7 패턴 3 및 Phase 7-ML에서 사용

### 4.3 IRF (충격반응함수)

상류 가격에 1단위 충격 발생 시 하류 가격의 시간별 반응 경로를 산출한다.

- **산출 기간**: 0~24개월 (`IRF_HORIZON = 24`)
- **피크 시점** = 정상 전달 시차 → Phase 7 패턴 1에서 "피크 시점 + 1개월 초과 시 시차 이탈" 판정 기준
- **피크 크기** = 전이탄력성 → Phase 7 패턴 2에서 전이율 정상 수준 참조값
- **95% 신뢰구간**: stderr 기반 (IRF ± 1.96 × 표준오차)

**피크 산출 방식**: IRF 절대값이 가장 큰 시점을 피크로 선택. 음수 반응(역방향 전달)도 포착하기 위해 절대값 기준 사용.

### 4.4 기준선 테이블

Phase 7 이상 탐지의 핵심 참조 파라미터.

| 필드 | 설명 | 사용처 |
| --- | --- | --- |
| `normal_transmission_lag` | IRF 피크 도달 시점 (개월) | 패턴 1 시차 이탈 판정 기준 |
| `transmission_elasticity` | IRF 피크 크기 | 패턴 2 전이율 정상 수준 참조 |
| `warmup_end` | 분석 시작 + 48개월 | Phase 7 탐지 시작 시점 (이전은 기준 분포 축적) |

### 4.5 추정 결과 요약

33개 구간 전부 추정 성공 (실패 0건). VECM 19개, VAR 14개로 Phase 3 모형 선택과 정확히 일치한다.

### 4.6 IRF 피크 시점 (정상 전달 시차) 분포

| 피크 시점 | 구간 수 | 비율 | 해당 모형 |
| --- | --- | --- | --- |
| 1개월 | 8개 | 24.2% | VAR 7개 + VECM 1개 |
| 2개월 | 7개 | 21.2% | VAR 4개 + VECM 3개 |
| 3개월 | 3개 | 9.1% | VAR 2개 + VECM 1개 |
| 6~8개월 | 3개 | 9.1% | VECM 3개 |
| 18개월 | 1개 | 3.0% | VECM 1개 |
| 24개월 (최대) | 11개 | 33.3% | VECM 11개 |

뚜렷한 이분화가 보인다. VAR 구간은 피크가 1~3개월에 집중(14개 중 13개)되고, VECM 구간은 피크 분포가 넓지만 24개월에 집중되는 경향(19개 중 11개)이 있다.

VAR 구간의 피크가 짧은 이유는 변화율 데이터에서 추정하기 때문이다. 차분 데이터의 충격은 빠르게 감쇠하므로 단기 반응만 포착된다. VECM 구간의 피크가 긴 이유는 수준 데이터에서 추정하기 때문이다. 오차수정 메커니즘에 의해 충격이 장기 균형으로 천천히 수렴하면서 반응이 누적된다.

피크 24개월이 11개나 되는 건 IRF_HORIZON=24의 제약 때문일 수 있다. 실제 피크가 24개월 이후에 있을 가능성이 있으며, 이 구간들의 정상 전달 시차는 "24개월 이상"으로 해석해야 한다 (PL-P4-003 해당).

### 4.7 구간별 피크 시점 패턴

**구간 A (국제가 → 수입단가)**:

| 품목 | 모형 | 피크 | 탄력성 | 비고 |
| --- | --- | --- | --- | --- |
| 밀 | VECM | 18개월 | 0.000678 | — |
| 옥수수 | VECM | 24개월 | 0.000696 | HORIZON 도달 |
| 대두 | VECM | 24개월 | 0.000856 | HORIZON 도달 |
| 팜유 | VECM | 6개월 | 0.000992 | — |
| 설탕 | VECM | 8개월 | 0.000897 | — |
| 땅콩 | VECM | 7개월 | 0.000352 | — |
| 커피 | VAR | 2개월 | 0.158036 | — |
| 쇠고기 | VAR | 2개월 | 0.100904 | — |
| 바나나 | VAR | 2개월 | -0.154557 | 역방향 반응 |
| 오렌지 | VAR | 3개월 | -0.032414 | 역방향 반응 |

VECM 구간의 탄력성이 0.0003~0.001로 극단적으로 작은 건 단위 차이(원/톤 vs $/톤) 때문이다. 국제가 1원/톤 변동은 무시할 수 있는 크기이므로 수입단가 반응도 당연히 작다. VAR 구간의 탄력성은 0.03~0.16 범위로 비교적 해석 가능한 크기인데, 이는 VAR이 변화율(%) 데이터를 사용하기 때문에 단위 문제가 상대적으로 적기 때문이다.

팜유·설탕·땅콩의 VECM 피크가 6~8개월인 건 주목할 만하다. 이 품목들은 국제가 충격이 수입단가에 전달되는 데 약 반년이 걸린다는 뜻이다. 반면 밀·옥수수·대두는 24개월 이상으로, 전달이 매우 느리게 누적되는 구조다. 이 차이는 수입 계약 구조에서 기인할 수 있다. 팜유·설탕은 현물 시장 비중이 높고, 밀·옥수수·대두는 장기 선도 계약 비중이 높아서 가격 조정이 느릴 수 있다.

바나나·오렌지의 음수 탄력성(-0.155, -0.032)은 국제가가 오를 때 수입단가가 오히려 떨어지는 역방향 반응이다. 열대 과일의 산지 다양성(필리핀·에콰도르·남아공 등)으로 특정 산지 가격 상승 시 다른 산지로 전환하는 효과가 반영된 것으로 추정된다.

**구간 B (수입단가 → PPI)**:

| 품목 | 모형 | 피크 | 탄력성 | 비고 |
| --- | --- | --- | --- | --- |
| 밀 | VECM | 24개월 | 0.116299 | HORIZON 도달 |
| 옥수수 | VECM | 24개월 | 0.160749 | HORIZON 도달 |
| 대두 | VECM | 24개월 | 0.161029 | HORIZON 도달 |
| 팜유 | VECM | 24개월 | 0.071407 | HORIZON 도달 |
| 설탕 | VECM | 24개월 | 0.060549 | HORIZON 도달 |
| 바나나 | VECM | 2개월 | -0.059734 | 역방향 반응 |
| 오렌지 | VECM | 24개월 | 0.036678 | HORIZON 도달 |
| 커피 | VAR | 2개월 | 0.038590 | — |
| 쇠고기 | VAR | 3개월 | -0.163720 | 역방향 반응 |
| 땅콩 | VAR | 2개월 | 0.043465 | — |

VECM 구간 B에서 피크 24개월이 6개(밀·옥수수·대두·팜유·설탕·오렌지)나 된다. 수입단가에서 PPI로의 전달이 느리게 누적되는 공통 패턴이다. 제분·정유·정제 등 가공 단계를 거치면서 원재료 가격 변동이 천천히 생산자 물가에 반영되는 구조를 보여준다.

바나나 구간 B만 VECM이면서 피크 2개월로 짧은 건 바나나가 가공 없이 거의 원형 그대로 유통되기 때문이다. 수입단가 변동이 PPI(과실류)에 2개월 만에 반영된다.

쇠고기 구간 B의 탄력성 -0.164는 전 VAR 구간 중 절대값이 가장 큰 역방향 반응이다. 수입단가가 오르면 PPI가 오히려 떨어지는데, 원산지 불일치(Pink Sheet: 뉴질랜드산, 수입: 미국·호주산) 때문에 두 가격이 같은 방향으로 움직이지 않는 것으로 해석된다.

**구간 D' (PPI → CPI)**:

| 품목 | 모형 | 피크 | 탄력성 | 비고 |
| --- | --- | --- | --- | --- |
| 밀 | VECM | 24개월 | 0.924105 | 약 1:1 전달 |
| 옥수수 | VECM | 24개월 | 1.131934 | 과대 전달 |
| 대두 | VAR | 1개월 | 0.035359 | — |
| 팜유 | VAR | 3개월 | 0.254109 | — |
| 설탕 | VAR | 1개월 | 0.260283 | — |
| 커피 | VAR | 1개월 | 0.007134 | — |
| 쇠고기 | VAR | 1개월 | -0.045618 | 역방향 반응 |

밀·옥수수의 VECM 탄력성이 0.92~1.13으로 약 1에 가깝다. 이건 "PPI가 1단위 오르면 CPI도 거의 1단위 오른다"는 뜻으로, 밀가루·전분이 단순 가공 제품이라 PPI→CPI 전달이 거의 1:1로 이루어지는 구조를 반영한다. 옥수수의 1.13은 과대 전달(over-transmission)로, PPI 상승분 이상이 CPI에 전가된다는 뜻이다. 이는 스낵과자(옥수수 CPI 대응 품목)의 가격 인상 시 원재료비 이상의 인상이 이루어질 수 있다는 해석이 가능하다.

VAR 구간은 피크가 전부 1~3개월로 짧고 탄력성도 작다(0.007~0.260). 공적분 없이 단기 관계만 포착되기 때문에 전달 효과가 빠르게 소멸하는 것으로 나타난다.

**구간 C·D (4구간 품목)**:

| 품목 | 구간 | 모형 | 피크 | 탄력성 | 비고 |
| --- | --- | --- | --- | --- | --- |
| 땅콩 | C | VAR | 1개월 | 0.522126 | — |
| 땅콩 | D | VAR | 1개월 | -0.013586 | 역방향 반응 |
| 바나나 | C | VECM | 2개월 | -1.272237 | 절대값 2위, 역방향 |
| 바나나 | D | VECM | 1개월 | 0.007385 | — |
| 오렌지 | C | VECM | 24개월 | 5.175378 | 절대값 1위, 극단적 증폭 |
| 오렌지 | D | VECM | 24개월 | 0.013402 | HORIZON 도달 |

오렌지 구간 C의 탄력성 5.175는 전 구간 중 절대값이 가장 크다. PPI의 작은 변동이 도매가에서 5배 이상 증폭되어 나타나는데, 과실류 합산 PPI의 노이즈 영향이다. PPI에는 사과·배·감귤 등 국내 제철 과일이 포함되어 있어서 PPI 변동 중 오렌지 도매가와 무관한 성분이 많고, 이 무관한 변동 대비 도매가 반응이 상대적으로 크게 나타나는 것이다. 피크도 24개월로 길어서 이 수치 자체를 기준선으로 사용하기에는 주의가 필요하다.

바나나 구간 C의 탄력성 -1.272는 전 구간 중 절대값이 두 번째로 크다. PPI(과실류)가 올라가면 바나나 도매가가 크게 떨어지는 역방향 반응인데, 국산 제철 과일 출하 증가 → 과실류 PPI 상승 → 대체재 효과로 수입 바나나 수요 감소 → 도매가 하락이라는 메커니즘으로 설명 가능하다. 이는 바나나·오렌지 PPI가 개별 품목이 아닌 '과실류' 합산 항목이라는 데이터 한계에서 기인한다.

### 4.8 시차(lag) 분포

| 시차 | VECM | VAR | 합계 | 비율 |
| --- | --- | --- | --- | --- |
| 1 | 3개 | 2개 | 5개 | 15.2% |
| 2 | 6개 | 4개 | 10개 | 30.3% |
| 3 | 10개 | 4개 | 14개 | 42.4% |
| 4 | 0개 | 4개 | 4개 | 12.1% |

VECM은 시차 2~3에 집중(16/19)되고, VAR은 비교적 고르게 분포한다. VECM의 시차가 VAR보다 작은 경향이 있는 건 `johansen_lag = var_lag_aic - 1` 때문이다.

VAR에서 시차 4가 나온 4개 구간(쇠고기 A·B·D', 바나나 A)은 단기 동적 관계가 복잡하다는 뜻으로, 4개월 전까지의 과거값이 현재 가격에 영향을 미친다는 것이다. 쇠고기가 3개 구간에서 모두 시차 4인 건 축산물 특유의 복잡한 유통 구조(도축·가공·냉동·해동 과정)가 반영된 결과로 볼 수 있다.

### 4.9 ECT/로그 스프레드 현황

| ECT 유형 | 구간 수 | Phase 7 패턴 3 적용 방식 |
| --- | --- | --- |
| ECT (VECM) | 19개 | ECT 절대값 연속 확대 판정 |
| log_spread (VAR) | 14개 | 스프레드 연속 방향성 판정 |

구간 B(수입단가→PPI)만 패턴 3의 적용 대상인데, 10개 품목 중 VECM 7개, VAR 3개다. VECM 7개 구간에서는 ECT 기반의 이론적으로 정밀한 스프레드 모니터링이 가능하고, VAR 3개 구간(커피·쇠고기·땅콩)에서는 로그 스프레드 기반의 대체 모니터링을 수행한다.

패턴 2(비대칭 전달)에서도 분기가 발생한다. VECM 구간에서는 TECM(ECT를 양수/음수로 분리)을 적용하고, VAR 구간에서는 비대칭 VAR(상승/하락 더미 교차항)을 적용한다. 구간 A·B에서 VECM 13개, VAR 7개이므로 TECM 적용 비율이 65%다.

### 4.10 결과 종합

Phase 4 결과에서 확인된 가격 전달 구조의 핵심 특징은 세 가지다.

첫째, 구간 A·B의 곡물 5종은 VECM으로 장기 균형이 확인되고 전달 시차가 길다(6~24개월). 이는 선도 계약 기반 수입 구조와 가공 단계의 가격 조정 지연을 반영한다.

둘째, 구간 D'(PPI→CPI)는 대부분 VAR로 단기 전달(1~3개월)만 포착된다. 소매 단계의 구조적 완충 효과를 보여주며, 밀·옥수수만 예외적으로 1:1에 가까운 장기 전달이 확인된다. 옥수수의 과대 전달(1.13)은 스낵과자 가격 인상 시 원재료비 이상의 전가가 이루어질 수 있다는 시사점을 준다.

셋째, 바나나·오렌지의 과실류 합산 PPI 문제가 구간 B·C에서 비직관적 결과(음수 탄력성, 극단적 탄력성)를 유발한다. 오렌지 구간 C(5.175)와 바나나 구간 C(-1.272)는 ECOS 데이터 구조의 한계에서 기인하며, 해당 구간의 Phase 7 탐지 결과 해석 시 반드시 고려해야 한다.

---

## 5. 산출물 구조

### 5.1 Phase 3 산출물

```
data/processed/phase3/
├── cointegration_results.csv       ← 33개 구간 공적분 검정 결과 테이블
└── model_routing.json              ← 품목·구간별 모형 경로 + 시차 ← Phase 4 주 입력
```

### 5.2 Phase 4 산출물

```
data/processed/phase4/
├── model_params/                   ← 33개 모형 파라미터 JSON
│   └── {cid}_{seg}_model.json
├── irf/                            ← 33개 IRF 시계열 CSV (0~24개월, 95% CI)
│   └── {cid}_{seg}_irf.csv
├── baseline/                       ← 33개 기준선 JSON (Phase 7 핵심 참조)
│   └── {cid}_{seg}_baseline.json
├── ect/                            ← 33개 ECT/로그 스프레드 CSV
│   └── {cid}_{seg}_ect.csv
└── phase4_summary.csv              ← 전 품목·구간 추정 요약
```

---

## 6. CSV/JSON 컬럼 설명

### 6.1 cointegration_results.csv

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| `commodity_id` | str | 품목 식별자 |
| `segment` | str | 분석 구간 (A, B, C, D, D_prime) |
| `upstream` | str | 상류 가격 컬럼명 |
| `downstream` | str | 하류 가격 컬럼명 |
| `n_obs` | int | 관측치 수 |
| `var_lag_aic` | int | VAR 최적 시차 (AIC 기준) |
| `var_lag_bic` | int | VAR 최적 시차 (BIC 기준) |
| `johansen_lag` | int | Johansen 검정 시차 (var_lag_aic - 1, 최소 1) |
| `trace_stat_r0` | float | Trace 통계량 (r=0 검정) |
| `trace_crit_r0` | float | Trace 임계값 (5%) |
| `trace_reject_r0` | bool | Trace 기각 여부 |
| `eigen_stat_r0` | float | Max-Eigenvalue 통계량 |
| `eigen_crit_r0` | float | Max-Eigen 임계값 (5%) |
| `eigen_reject_r0` | bool | Max-Eigen 기각 여부 |
| `cointegrated` | bool | 공적분 존재 여부 (최종 판정) |
| `judgment_note` | str | 판정 근거 설명 |
| `model_selected` | str | 선택 모형 (VECM / VAR) |
| `integration_flag` | str / NaN | I(2) 포함 시 주의 메시지, 정상 쌍은 NaN |

### 6.2 model_routing.json

```json
{
  "wheat": {
    "A": {
      "model": "VECM",
      "cointegrated": true,
      "upstream": "intl_price_krw",
      "downstream": "import_price_usd",
      "var_lag_aic": 4,
      "johansen_lag": 3
    }
  }
}
```

Phase 4에서 VECM 구간은 `johansen_lag`를, VAR 구간은 `var_lag_aic`를 시차로 사용한다.

### 6.3 model_params/{cid}_{seg}_model.json

```json
{
  "commodity_id": "wheat",
  "segment": "A",
  "upstream_col": "intl_price_krw",
  "downstream_col": "import_price_usd",
  "lag_selection_criterion": "AIC",
  "model_type": "VECM",
  "lag_selected": 3,
  "n_obs": 300,
  "cointegrated": true,
  "det_order": 0,
  "coint_rank": 1,
  "alpha": [[-0.014], [0.000083]],
  "beta": [[1.0], [-1359.84]]
}
```

VECM에만 `alpha`(조정 계수), `beta`(공적분 벡터) 포함. VAR에는 `aic`, `bic` 포함.

### 6.4 irf/{cid}_{seg}_irf.csv

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| `horizon` | int | 충격 후 경과 기간 (개월, 0~24) |
| `irf_downstream` | float | 하류 가격의 상류 충격에 대한 반응 |
| `irf_lower_ci` | float | IRF 95% 신뢰구간 하한 |
| `irf_upper_ci` | float | IRF 95% 신뢰구간 상한 |
| `irf_peak_horizon` | int | IRF 피크 도달 시점 (모든 행에 동일값) |
| `irf_peak_magnitude` | float | IRF 피크 크기 (모든 행에 동일값) |

### 6.5 baseline/{cid}_{seg}_baseline.json

```json
{
  "commodity_id": "wheat",
  "segment": "A",
  "normal_transmission_lag": 18,
  "transmission_elasticity": 0.000678,
  "warmup_end": "2004-01",
  "model_type": "VECM",
  "estimation_period_start": "2000-01",
  "estimation_period_end": "2024-12",
  "n_obs": 300
}
```

### 6.6 ect/{cid}_{seg}_ect.csv

모든 구간에서 생성 (VECM·VAR 모두).

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| `date` | DatetimeIndex (MS) | 월 기준일 (인덱스) |
| `ect` | float | ECT 값 (VECM) 또는 로그 수준 스프레드 (VAR) |
| `ect_type` | str | `"ECT"` (VECM) 또는 `"log_spread"` (VAR) |

VECM 구간의 ECT = β' × Y_t (공적분 벡터 × 수준 데이터). 장기 균형으로부터의 이탈을 직접 측정.
VAR 구간의 로그 스프레드 = log(하류_sa) - log(상류_sa). ECT 대체 지표.

Phase 7 패턴 3(안정기 스프레드 누적 확대) 및 Phase 7-ML 입력 피처(6종 중 하나)로 사용.

### 6.7 phase4_summary.csv

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| `commodity_id` | str | 품목 식별자 |
| `segment` | str | 분석 구간 |
| `model_type` | str | VECM / VAR |
| `lag` | int | 사용된 시차 |
| `n_obs` | int | 관측치 수 |
| `peak_horizon` | int | IRF 피크 시점 (개월) |
| `peak_magnitude` | float | IRF 피크 크기 (전이탄력성) |
| `ect_type` | str | ECT / log_spread |
| `estimation_start` | str | 추정 시작 월 (YYYY-MM) |
| `estimation_end` | str | 추정 종료 월 (YYYY-MM) |

---

## 7. 데이터 리니지

Phase 0~4의 데이터 변환 단계를 정리한다:

```
[Phase 0] merged/{cid}.csv
    │
    ▼  Phase 1: STL 분해
[_sa]  계절 조정 수준 데이터
[_pct] 전월 대비 변화율
    │
    ▼  Phase 2: ADF+KPSS
integration_orders.json (적분 차수)
    │
    ▼  Phase 3: Johansen 공적분 검정
model_routing.json (VECM/VAR 분기 + 시차)
    │
    ▼  Phase 4: 모형 추정
    │
    ├─ VECM 구간: _sa 수준 데이터 입력
    │   ├─ ECT = β' × Y_t → ect/{cid}_{seg}_ect.csv (ect_type="ECT")
    │   ├─ IRF → irf/{cid}_{seg}_irf.csv
    │   └─ 기준선 → baseline/{cid}_{seg}_baseline.json
    │
    ├─ VAR 구간: _pct 변화율 데이터 입력
    │   ├─ log_spread = log(하류_sa) - log(상류_sa) → ect/{cid}_{seg}_ect.csv (ect_type="log_spread")
    │   ├─ IRF → irf/{cid}_{seg}_irf.csv
    │   └─ 기준선 → baseline/{cid}_{seg}_baseline.json
    │
    ▼  후속 Phase 사용처
    ├─ baseline → Phase 7 패턴 1 (시차 기준), 패턴 2 (전이율 참조)
    ├─ irf → Phase 7 패턴 1 (IRF 피크 = 정상 전달 시차)
    ├─ ect → Phase 7 패턴 3 (스프레드 모니터링), Phase 7-ML 피처
    └─ model_routing → Phase 5 (Granger 인과 방향 갱신)
```

---

## 8. 주의사항

### IRF 피크 24개월 도달 케이스

VECM 구간 B에서 대부분 피크가 24개월(최대치)에 찍힌다. 이는 충격 반응이 천천히 누적되면서 IRF_HORIZON 내에 수렴하지 않은 경우다. exception_spec의 PL-P4-003에 해당하며, 해당 구간의 정상 전달 시차는 "≥ 24개월"로 해석해야 한다. Phase 7 패턴 1에서 시차 이탈 판정 시 이 점을 고려한다.

### 구간 A 탄력성의 단위 의존성

구간 A(국제가→수입단가)의 VECM 탄력성이 매우 작은 값(0.0003~0.001)으로 나온다. 이는 국제가(원/톤)와 수입단가($/톤)의 단위가 다르기 때문이며, 가격 전달이 약하다는 뜻이 아니다. Phase 7에서는 탄력성 절대 크기보다 피크 시점(시차)과 방향(부호)을 주로 사용한다.

### 음수 탄력성

쇠고기 구간 B(-0.164), 바나나 구간 A(-0.155), 바나나 구간 C(-1.272) 등에서 음수 탄력성이 나타난다. 이는 상류 가격 상승 시 하류 가격이 오히려 떨어지는 역방향 반응을 의미하며, 원산지 불일치, 과실류 합산 PPI 노이즈, 또는 단기적 대체재 전환 효과가 원인일 수 있다.

### ECT vs 로그 스프레드

ECT는 공적분 벡터에 의해 이론적으로 정의된 균형 이탈 지표이므로 해석이 명확하다. 로그 스프레드는 공적분이 없는 구간의 대체 지표이며, 두 가격의 로그 비율 변화를 추적한다. Phase 7 패턴 3에서 두 지표의 해석 방식이 다르므로 `ect_type` 필드를 확인하여 분기해야 한다.

---

## 9. 다음 단계

Phase 4 완료 후 다음 단계로 진행:

- **Phase 5** (인과 방향 확정): 4구간 품목(땅콩·바나나·오렌지) 구간 C에 대해 Granger 양방향 검정 수행. `model_routing.json`의 `granger_direction` 갱신.
- **Phase 6** (구조 변화 탐지): Bai-Perron 자동 탐지 + Chow Test(2008·2020·2022) 교차 확인. 하위 기간 분할 및 재추정.
- **Phase 7** (이상 패턴 탐지): Phase 4 기준선을 참조하여 3종 패턴 통계 탐지 수행.
